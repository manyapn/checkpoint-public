Environment for every artifact in docs/reports/
Captured 2026-08-04, on the machine that produced them, from the same session.

MACHINE
  uname -a        Linux 1e492725b67a 6.12.76-linuxkit #1 SMP Fri Apr 17 14:56:37 UTC 2026 aarch64 GNU/Linux
  arch            aarch64 (arm64)
  CPU(s)          10 online (lscpu: Vendor ID "Apple", 1 cluster, 1 thread per core)
  MemTotal        8126672 kB
  container       Docker (/.dockerenv present; root filesystem is a containerd overlayfs)
  privileges      uid 0 with CAP_SYS_ADMIN (fanotify_init needs it)

TOOLCHAIN
  go              go version go1.24.13 linux/arm64
  gcc             gcc (Debian 12.2.0-14+deb12u1) 12.2.0
  git             git version 2.39.5
  distro          Debian GNU/Linux 12 (bookworm)

BINARY UNDER TEST
  commit          f07cd88398dfc8fa0039180df33e94db6dfa5051
  commit time     2026-08-04T19:25:52Z
  built from      a clean checkout of that commit, so `checkpoint version` carries no
                  "MODIFIED working tree" mark and the artifacts are unambiguous
  build command   go build -o <bin> ./cmd/checkpoint
  bench harness   go build -o <bench> ./bench, from the same checkout

FILESYSTEMS
  under test, good case:  /mnt/ckext4, ext4 on a loopback image
      created with   dd if=/dev/zero of=/var/ckbench/ext4.img bs=1M count=4096
                     mkfs.ext4 -F /var/ckbench/ext4.img
                     mount -o loop /var/ckbench/ext4.img /mnt/ckext4
      mount line     /var/ckbench/ext4.img on /mnt/ckext4 type ext4 (rw,relatime)
      size           /dev/loop6, ext4, 3.9G
      cleanup        unmounted and the image deleted after the runs

  under test, degraded case:  /tmp, overlayfs (the container root filesystem)
      mount type     overlay on / type overlay (rw,relatime,
                     lowerdir=/var/lib/desktop-containerd/...,
                     upperdir=/var/lib/desktop-containerd/.../73/fs)
      statfs         /tmp reports magic 0x794c7630, which checkpoint names "overlayfs"

  overlayfs provides no dirent change feed, so delete provenance is unavailable
  there. That is why the same binary produces a SKIP in the selftest and a 0%
  agent_delete_undo column in the benchmark on /tmp, and full results on
  /mnt/ckext4. It is the filesystem, not a regression.
